import 'package:flutter/material.dart';

class DashboardNoitification extends StatelessWidget {
  const DashboardNoitification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: Column(children: [Text("notification page")]),
    );
  }
}
